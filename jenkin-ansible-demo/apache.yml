- hosts: all
  become: true
  tasks:
    - name: Install nginx
      yum:
        name: nginx
        state: present

    - name: Start nginx service
      service:
        name: nginx
        state: started
        enabled: true

    - name: Deploy static website
      copy:
        src: index.html
        dest: /usr/share/nginx/html/index.html
